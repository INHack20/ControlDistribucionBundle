{% extends 'INHack20ControlDistribucionBundle:Principal:index.html.twig' %}

{% block contenido %}
    <h1>Distribucion Lista de </h1>

    <table class="records_Lista de ">
        <thead>
            <tr>
                <th>Id</th>
                <th>Tribunal</th>
                <th>Causa</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('distribucion_Ver', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.tribunal.descripcion }}</td>
                <td>{{ entity.causa.nombre }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('distribucion_Ver', { 'id': entity.id }) }}">Ver</a>
                        </li>
                        <li>
                            <a href="{{ path('distribucion_Editar', { 'id': entity.id }) }}">Editar</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('distribucion_new') }}">
                Registrar
            </a>
        </li>
    </ul>
{% endblock %}