<h1>Tribunal list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nro</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('tribunal_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>Tribunal {{ entity.nro }} De {{ entity.tribunaltipo.nombre }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('tribunal_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tribunal_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('tribunal_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
